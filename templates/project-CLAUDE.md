# CLAUDE.md

<!--
  This project lives as a subfolder of the workflow starter pack, so it AUTOMATICALLY inherits the
  parent baseline (../CLAUDE.md) — Claude Code loads ancestor CLAUDE.md files at launch. You do NOT
  need to restate the workflow rules here. This file is for THIS project's facts and any deliberate
  overrides only. Because it's read after the parent baseline, anything here wins on conflict.
-->

## Project status

_TODO: what this project is, what's built, where the spec/PRD lives (knowledge/product_docs/)._

## Commands

_TODO: dev / build / lint / test / migrate commands, seed creds, env vars._

## Code layout

_TODO: the binding directory map — routes, business logic, seams/providers, glue, design tokens._

## Project-specific rule overrides

<!-- Optional. Only where this project deliberately diverges from the parent baseline.
     Read after the baseline, so statements here win on conflict. Leave empty if none. -->
_None._
