# Style Guide — single source of truth for design tokens

Every surface must stay consistent with the values here. Before styling anything new, check this file and reuse
what's present; if a genuinely new value is needed, add it here in the same change and log the reasoning in
`decisions.md`. Don't invent values speculatively — fill sections in as real decisions are made.

## Palette
_TODO: primary / secondary / neutrals / semantic (success, warning, error) with hex + token names._

## Typography
_TODO: font family/families, type scale, weights, line-heights._

## Spacing & layout
_TODO: spacing scale, container widths, breakpoints._

## Radii, shadows, borders
_TODO._

## Tokens ↔ code
_TODO: where these tokens live in code (e.g. a theme file / CSS `@theme` block / config) so the doc and the
implementation stay in sync._
