<!--
  TEMPLATE — copy this folder to `<real-domain>-feature-brief/` and this file to
  `<real-name>-high_level_feature_brief.md`. `<domain>` is the business/domain aspect (e.g.
  inbox-messaging, payments, records-reporting), NOT a UI component or code module. Check the registry
  (`../index.md`) and existing domain folders first — extend an existing brief rather than duplicating.
  Delete these comments once filled in.
-->

# <Feature name> — High-Level Feature Brief

**Domain:** `<domain>`
**Status:** backlog | in-progress | done  ·  (keep in sync with the row in `../index.md`)

## Problem
_What user/business problem does this solve? Who feels it?_

## Scope
_What's in, what's explicitly out (for now)._

## Tie-back to product docs
_Which PRD/product-spec section this comes from._

## Domain entities & services touched
_Data models, services, seams/providers this feature reads or changes._

## Stories
_Break user-facing capabilities into files under `stories/`. Link them here as they're written._
- [ ] `stories/<story-slug>.md`
